export type FileDataRecord = {
  eventTime: string;
  tankLevelPercentage: number;
  tankLevel: number;
};

export type TankDataRecord = {
  eventTime: Date;
  tankLevelPercentage: number;
  tankLevel: number;
};
